name: nomba-gateway

services:
  db:
    image: postgres:16-alpine
    container_name: nomba-gateway-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-David*2020*}
      POSTGRES_DB: ${POSTGRES_DB:-nomba_gateway}
    volumes:
      - pg_data:/var/lib/postgresql/data
      - ./schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-nomba_gateway}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - nomba-gateway-net

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nomba-gateway-app
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: postgresql://postgres:baEuTYTjvJzyhYZfILnVYvJGkRyiOoAa@postgres.railway.internal:5432/railway
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "${APP_PORT:-8000}:8000"
    volumes:
      - ./app:/code/app
      - ./tests:/code/tests
    networks:
      - nomba-gateway-net

volumes:
  pg_data:
    driver: local

networks:
  nomba-gateway-net:
    driver: bridge
